import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF4CE5B1); // Adjusted from image (greenish teal)
  static const Color primaryGreen = Color(0xFF4CE579); // The vibrant green button
  static const Color background = Color(0xFFF3F4F6); // Light grayish background
  static const Color cardBackground = Colors.white;
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  
  // Specific UI colors
  static const Color pinkIconBg = Color(0xFFF9A8D4); // Bank icon bg
  static const Color blueIconBg = Color(0xFF3B82F6); // Paypal icon bg
  static const Color greenIconBg = Color(0xFF4ADE80); // Card icon bg
}
